rules of file and directory name:
   1. First characters of words must be capital. However, if the words are used as conjection, you mustn't do.
   2. If the files are in top directory (YUKIKAZE/~), ALL LETTERS in filename have to be capital.

"#" means describing functions of directories and files in the left of "#"

/main #It is the directory of main program
   |---- Main.cc
   |---- ConvertCSStoCPP.cc
   |---- FileProcessing.cc
   /setings
      |---- ComputerSetting.conf
      |---- InitialSetting.cc